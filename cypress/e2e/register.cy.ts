describe("registration spec", () => {
  it("should show error validations when leaving all fields blank", () => {
    cy.visit("/dashboard-list/");
    // cy.get('#toggle-big-register-landing-drawer-btn').click();
  });
});
